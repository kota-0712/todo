<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sample.app.dao.mapper.TaskMapper">

    <select id="findAll" resultType="sample.app.dao.entity.Task">
        SELECT id, user_id, title, content, name,
               start_date, end_date, created_at, updated_at
        FROM tasks
        ORDER BY id DESC
    </select>

    <select id="findById" resultType="sample.app.dao.entity.Task">
        SELECT id, user_id, title, content, name,
               start_date, end_date, created_at, updated_at
        FROM tasks
        WHERE id = #{id}
    </select>

    <select id="findByUserId" resultType="sample.app.dao.entity.Task">
        SELECT id, user_id, title, content, name,
               start_date, end_date, created_at, updated_at
        FROM tasks
        WHERE user_id = #{userId}
        ORDER BY id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tasks (user_id, title, content, name, start_date, end_date, created_at, updated_at)
        VALUES (#{userId}, #{title}, #{content}, #{name}, #{startDate}, #{endDate}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="update">
        UPDATE tasks
        SET title = #{title},
            content = #{content},
            name = #{name},
            start_date = #{startDate},
            end_date = #{endDate},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM tasks WHERE id = #{id}
    </delete>

</mapper>