<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sample.app.dao.mapper.UserMapper">

    <select id="findByUsername" resultType="sample.app.dao.entity.User">
        SELECT id, username, password, created_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findById" resultType="sample.app.dao.entity.User">
        SELECT id, username, password, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, created_at)
        VALUES (#{username}, #{password}, CURRENT_TIMESTAMP)
    </insert>

</mapper>